#Auto-Genereated by SE352
.data
	.input_fmtstr:	.string	"%d"			# (main.c, 107)
	.output_fmtstr:	.string	"%d\012"			# (main.c, 108)
	.comm	year,4			# (decl.c, 183)
	.comm	month,4			# (decl.c, 183)
	.comm	day,4			# (decl.c, 183)

.text
.globl  SQAless
SQAless:
	movl 8(%esp), %eax			# (main.c, 53)
	cmpl %eax, 4(%esp)			# (main.c, 54)
	setl    %al			# (main.c, 55)
	movzbl  %al, %eax			# (main.c, 56)
	ret			# (main.c, 57)

.text
.globl  SQAlarger
SQAlarger:
	movl 8(%esp), %eax			# (main.c, 53)
	cmpl %eax, 4(%esp)			# (main.c, 54)
	setg    %al			# (main.c, 55)
	movzbl  %al, %eax			# (main.c, 56)
	ret			# (main.c, 57)

.text
.globl  SQAequal
SQAequal:
	movl 8(%esp), %eax			# (main.c, 53)
	cmpl %eax, 4(%esp)			# (main.c, 54)
	sete    %al			# (main.c, 55)
	movzbl  %al, %eax			# (main.c, 56)
	ret			# (main.c, 57)

.text
.globl  SQAstore
SQAstore:
	movl 12(%esp), %eax			# (main.c, 74)
	movl 4(%esp), %ecx			# (main.c, 75)
	movl 8(%esp), %edx			# (main.c, 76)
	movl %eax, (%ecx,%edx)			# (main.c, 77)
	ret			# (main.c, 78)

.text
.globl  SQAload
SQAload:
	movl 4(%esp), %edx			# (main.c, 84)
	movl 8(%esp), %eax			# (main.c, 85)
	movl (%edx,%eax), %eax			# (main.c, 86)
	ret			# (main.c, 87)

.text
.globl	f
f:
	pushl %ebp			# (emit.c, 17)
	movl %esp, %ebp			# (emit.c, 22)
	subl $124, %esp			# (emit.c, 25)
	cmpl $0, 8(%ebp)			# (stmt.c, 345)
	je Label_0			# (stmt.c, 346)
	movl 8(%ebp), %eax			# (expr.c, 439)
	subl $1, %eax			# (expr.c, 440)
	movl %eax, -8(%ebp)			# (expr.c, 441)
	pushl -8(%ebp)			# (expr.c, 410)
	call f			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -4(%ebp)			# (expr.c, 420)
	movl -4(%ebp), %eax			# (expr.c, 431)
	addl 8(%ebp), %eax			# (expr.c, 432)
	movl %eax, -16(%ebp)			# (expr.c, 433)
	movl -16(%ebp), %eax			# (stmt.c, 452)
	jmp Label_1			# (stmt.c, 348)
Label_0:
	movl 8(%ebp), %eax			# (stmt.c, 452)
Label_1:
	movl %ebp, %esp			# (emit.c, 43)
	popl %ebp			# (emit.c, 47)
	ret			# (emit.c, 48)

.text
.globl	g
g:
	pushl %ebp			# (emit.c, 17)
	movl %esp, %ebp			# (emit.c, 22)
	subl $124, %esp			# (emit.c, 25)
	movl $0, %eax			# (stmt.c, 409)
	movl %eax, -4(%ebp)			# (stmt.c, 410)
Label_2:
	movl 12(%ebp), %eax			# (expr.c, 431)
	addl $1, %eax			# (expr.c, 432)
	movl %eax, -12(%ebp)			# (expr.c, 433)
	pushl -12(%ebp)			# (expr.c, 410)
	pushl 8(%ebp)			# (expr.c, 410)
	call SQAless			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -8(%ebp)			# (expr.c, 420)
	cmpl $0, -8(%ebp)			# (stmt.c, 384)
	je Label_3			# (stmt.c, 385)
	movl -4(%ebp), %eax			# (expr.c, 431)
	addl 8(%ebp), %eax			# (expr.c, 432)
	movl %eax, -16(%ebp)			# (expr.c, 433)
	movl -16(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -4(%ebp)			# (stmt.c, 410)
	movl 8(%ebp), %eax			# (expr.c, 431)
	addl 16(%ebp), %eax			# (expr.c, 432)
	movl %eax, -20(%ebp)			# (expr.c, 433)
	movl -20(%ebp), %eax			# (stmt.c, 409)
	movl %eax, 8(%ebp)			# (stmt.c, 410)
	jmp Label_2			# (stmt.c, 387)
Label_3:
	movl -4(%ebp), %eax			# (stmt.c, 452)
	movl %ebp, %esp			# (emit.c, 43)
	popl %ebp			# (emit.c, 47)
	ret			# (emit.c, 48)

.text
.globl	printStrs
printStrs:
	pushl %ebp			# (emit.c, 17)
	movl %esp, %ebp			# (emit.c, 22)
	subl $124, %esp			# (emit.c, 25)
	movl $0, %eax			# (stmt.c, 409)
	movl %eax, -4(%ebp)			# (stmt.c, 410)
	movl -4(%ebp), %eax			# (expr.c, 447)
	imull $4, %eax			# (expr.c, 448)
	movl %eax, -16(%ebp)			# (expr.c, 449)
	pushl -16(%ebp)			# (expr.c, 410)
	pushl 8(%ebp)			# (expr.c, 410)
	call SQAload			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -12(%ebp)			# (expr.c, 420)
	movl -12(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -8(%ebp)			# (stmt.c, 410)
Label_4:
	cmpl $0, -8(%ebp)			# (stmt.c, 384)
	je Label_5			# (stmt.c, 385)
	pushl -8(%ebp)			# (expr.c, 410)
	call puts			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -20(%ebp)			# (expr.c, 420)
	movl -4(%ebp), %eax			# (expr.c, 431)
	addl $1, %eax			# (expr.c, 432)
	movl %eax, -24(%ebp)			# (expr.c, 433)
	movl -24(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -4(%ebp)			# (stmt.c, 410)
	movl -4(%ebp), %eax			# (expr.c, 447)
	imull $4, %eax			# (expr.c, 448)
	movl %eax, -32(%ebp)			# (expr.c, 449)
	pushl -32(%ebp)			# (expr.c, 410)
	pushl 8(%ebp)			# (expr.c, 410)
	call SQAload			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -28(%ebp)			# (expr.c, 420)
	movl -28(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -8(%ebp)			# (stmt.c, 410)
	jmp Label_4			# (stmt.c, 387)
Label_5:
	movl %ebp, %esp			# (emit.c, 43)
	popl %ebp			# (emit.c, 47)
	ret			# (emit.c, 48)

.text
.globl	main
main:
	pushl %ebp			# (emit.c, 17)
	movl %esp, %ebp			# (emit.c, 22)
	subl $124, %esp			# (emit.c, 25)
	pushl $40			# (expr.c, 410)
	call malloc			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -20(%ebp)			# (expr.c, 420)
	movl -20(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -8(%ebp)			# (stmt.c, 410)
	movl $0, %eax			# (stmt.c, 409)
	movl %eax, -12(%ebp)			# (stmt.c, 410)
Label_6:
	pushl $10			# (expr.c, 410)
	pushl -12(%ebp)			# (expr.c, 410)
	call SQAless			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -24(%ebp)			# (expr.c, 420)
	cmpl $0, -24(%ebp)			# (stmt.c, 384)
	je Label_7			# (stmt.c, 385)
	movl -12(%ebp), %eax			# (expr.c, 447)
	imull $4, %eax			# (expr.c, 448)
	movl %eax, -32(%ebp)			# (expr.c, 449)
	pushl -12(%ebp)			# (expr.c, 410)
	pushl -32(%ebp)			# (expr.c, 410)
	pushl -8(%ebp)			# (expr.c, 410)
	call SQAstore			# (expr.c, 414)
	addl $12, %esp			# (expr.c, 417)
	movl %eax, -28(%ebp)			# (expr.c, 420)
	movl -12(%ebp), %eax			# (expr.c, 431)
	addl $1, %eax			# (expr.c, 432)
	movl %eax, -36(%ebp)			# (expr.c, 433)
	movl -36(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -12(%ebp)			# (stmt.c, 410)
	jmp Label_6			# (stmt.c, 387)
Label_7:
	movl $0, %eax			# (stmt.c, 409)
	movl %eax, -12(%ebp)			# (stmt.c, 410)
Label_8:
	pushl $10			# (expr.c, 410)
	pushl -12(%ebp)			# (expr.c, 410)
	call SQAless			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -40(%ebp)			# (expr.c, 420)
	cmpl $0, -40(%ebp)			# (stmt.c, 384)
	je Label_9			# (stmt.c, 385)
	movl -12(%ebp), %eax			# (expr.c, 447)
	imull $4, %eax			# (expr.c, 448)
	movl %eax, -48(%ebp)			# (expr.c, 449)
	pushl -48(%ebp)			# (expr.c, 410)
	pushl -8(%ebp)			# (expr.c, 410)
	call SQAload			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -44(%ebp)			# (expr.c, 420)
	movl -44(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -16(%ebp)			# (stmt.c, 410)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl -16(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	movl -12(%ebp), %eax			# (expr.c, 431)
	addl $1, %eax			# (expr.c, 432)
	movl %eax, -52(%ebp)			# (expr.c, 433)
	movl -52(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -12(%ebp)			# (stmt.c, 410)
	jmp Label_8			# (stmt.c, 387)
Label_9:
	movl $97, %eax			# (stmt.c, 409)
	movl %eax, -16(%ebp)			# (stmt.c, 410)
	movl $0, %eax			# (stmt.c, 409)
	movl %eax, -12(%ebp)			# (stmt.c, 410)
Label_10:
	pushl $26			# (expr.c, 410)
	pushl -12(%ebp)			# (expr.c, 410)
	call SQAless			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -56(%ebp)			# (expr.c, 420)
	cmpl $0, -56(%ebp)			# (stmt.c, 384)
	je Label_11			# (stmt.c, 385)
	movl -16(%ebp), %eax			# (expr.c, 431)
	addl -12(%ebp), %eax			# (expr.c, 432)
	movl %eax, -64(%ebp)			# (expr.c, 433)
	pushl -64(%ebp)			# (expr.c, 410)
	pushl -12(%ebp)			# (expr.c, 410)
	pushl -8(%ebp)			# (expr.c, 410)
	call SQAstore			# (expr.c, 414)
	addl $12, %esp			# (expr.c, 417)
	movl %eax, -60(%ebp)			# (expr.c, 420)
	movl -12(%ebp), %eax			# (expr.c, 431)
	addl $1, %eax			# (expr.c, 432)
	movl %eax, -68(%ebp)			# (expr.c, 433)
	movl -68(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -12(%ebp)			# (stmt.c, 410)
	jmp Label_10			# (stmt.c, 387)
Label_11:
	pushl $0			# (expr.c, 410)
	pushl $26			# (expr.c, 410)
	pushl -8(%ebp)			# (expr.c, 410)
	call SQAstore			# (expr.c, 414)
	addl $12, %esp			# (expr.c, 417)
	movl %eax, -72(%ebp)			# (expr.c, 420)
	pushl -8(%ebp)			# (expr.c, 410)
	call puts			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -76(%ebp)			# (expr.c, 420)
	pushl -8(%ebp)			# (expr.c, 410)
	call free			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -80(%ebp)			# (expr.c, 420)
	pushl 12(%ebp)			# (expr.c, 410)
	call printStrs			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -84(%ebp)			# (expr.c, 420)
	pushl 16(%ebp)			# (expr.c, 410)
	call printStrs			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -88(%ebp)			# (expr.c, 420)
	movl $2022, %eax			# (stmt.c, 409)
	movl %eax, year			# (stmt.c, 410)
	movl $11, %eax			# (stmt.c, 409)
	movl %eax, month			# (stmt.c, 410)
	movl $14, %eax			# (stmt.c, 409)
	movl %eax, day			# (stmt.c, 410)
	pushl $100			# (expr.c, 410)
	call f			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -96(%ebp)			# (expr.c, 420)
	movl -96(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -92(%ebp)			# (stmt.c, 410)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl -92(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	pushl $1			# (expr.c, 410)
	pushl $100			# (expr.c, 410)
	pushl $1			# (expr.c, 410)
	call g			# (expr.c, 414)
	addl $12, %esp			# (expr.c, 417)
	movl %eax, -100(%ebp)			# (expr.c, 420)
	movl -100(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -92(%ebp)			# (stmt.c, 410)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl -92(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl year			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl month			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl day			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl 8(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	call fork			# (expr.c, 414)
	movl %eax, -108(%ebp)			# (expr.c, 420)
	movl -108(%ebp), %eax			# (stmt.c, 409)
	movl %eax, -104(%ebp)			# (stmt.c, 410)
	pushl $0			# (expr.c, 410)
	pushl -104(%ebp)			# (expr.c, 410)
	call SQAless			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -112(%ebp)			# (expr.c, 420)
	cmpl $0, -112(%ebp)			# (stmt.c, 345)
	je Label_12			# (stmt.c, 346)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl -104(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	jmp Label_15			# (stmt.c, 348)
Label_12:
	pushl $0			# (expr.c, 410)
	pushl -104(%ebp)			# (expr.c, 410)
	call SQAequal			# (expr.c, 414)
	addl $8, %esp			# (expr.c, 417)
	movl %eax, -116(%ebp)			# (expr.c, 420)
	cmpl $0, -116(%ebp)			# (stmt.c, 345)
	je Label_13			# (stmt.c, 346)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl -104(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
	jmp Label_14			# (stmt.c, 348)
Label_13:
	pushl $0			# (expr.c, 410)
	call wait			# (expr.c, 414)
	addl $4, %esp			# (expr.c, 417)
	movl %eax, -120(%ebp)			# (expr.c, 420)
	leal .output_fmtstr, %eax			# (stmt.c, 439)
	pushl -104(%ebp)			# (stmt.c, 440)
	pushl %eax			# (stmt.c, 441)
	call printf			# (stmt.c, 442)
	addl $8, %esp			# (stmt.c, 443)
Label_14:
Label_15:
	movl $0, %eax			# (stmt.c, 452)
	movl %ebp, %esp			# (emit.c, 43)
	popl %ebp			# (emit.c, 47)
	ret			# (emit.c, 48)
